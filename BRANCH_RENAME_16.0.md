# Branch rename to 16.0 (commands only)

These commands are documented intentionally and were not executed.

```bash
git branch -m main 16.0
git fetch origin
# optional if you want to publish 16.0 to remote
git push -u origin 16.0
# optional later if main should be removed on remote
# git push origin --delete main
```
